void main(){
  var person = {
    'name': 'Mahadi',
  'age': 24,
  'city': 'Bogura',};
  print(person);
  //addAll
  person.addAll({'country':'Bangladesh'}); //clear() add() addAll() remove() forEach()
  print(person);
  print(person.values);
  print(person.keys);
  print(person.isEmpty); //length isEmpty isNotEmpty

}